// next.config.mjs
import { config as dotenvConfig } from 'dotenv';
dotenvConfig();

// Log process.env for debugging
// console.log(process.env);

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
